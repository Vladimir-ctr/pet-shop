run:
  timeout: 2m
  tests: true

linters:
  enable:
    - unused
    - bodyclose
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - gocyclo

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
